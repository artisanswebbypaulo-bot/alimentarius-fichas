toml[build]
  functions = "netlify/functions"
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self' 'unsafe-inline' 'unsafe-eval' blob: data: https://api.anthropic.com"

Commit changes

O nome dos ficheiros no GitHub não muda — continua index.html e netlify.toml. O nome do ficheiro que descarregaste (ficha tecnica receita.html) é só o nome local no teu computador.
